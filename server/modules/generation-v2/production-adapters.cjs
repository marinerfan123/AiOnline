'use strict';
function createProductionAdapters({pgPool,dispatcher,assetFinalize,realtime}={}){return{async dispatchSingle(payload){if(!dispatcher?.generate)throw new Error('dispatcher.generate unavailable');const r=await dispatcher.generate(pgPool,{...payload,count:1});return r},async uploadToOss({providerUrl,objectKey,item}){if(!assetFinalize?.finalizeUrl)throw new Error('assetFinalize.finalizeUrl unavailable');const src=item.request_payload||{};const r=await assetFinalize.finalizeUrl(pgPool,{userId:item.user_id,taskId:item.batch_id,idx:item.item_index,providerUrl,type:item.content_type||'image',prompt:src.prompt||'',model:item.model_id||src.model||'',ratio:src.ratio||'1:1',pendingId:item.item_id,objectKey});if(!r||r.status!=='success'||!r.ossUrl)throw new Error((r&&r.error)||'asset finalize failed');return{ossUrl:r.ossUrl,mediaId:r.mediaId}},async publish(event){const userId=event.payload&&event.payload.userId;if(!userId)return false;realtime?.emitTaskUpdate?.(userId,{type:event.event_type,aggregateId:event.aggregate_id,...event.payload});return true}}}
module.exports={createProductionAdapters};
