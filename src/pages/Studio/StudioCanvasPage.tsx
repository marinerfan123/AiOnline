// M05-A — Studio Canvas Page.
// Entry point for /studio/:projectId — renders the full IDE layout via StudioPage.
import StudioPage from '@/features/studio-v2/StudioPage';

export default function StudioCanvasPage() {
  return <StudioPage />;
}
